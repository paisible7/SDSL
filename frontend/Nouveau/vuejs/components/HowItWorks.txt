<script setup>
import { Phone, ClipboardList, CheckCircle2 } from 'lucide-vue-next'
import { markRaw } from 'vue'

const steps = [
  {
    icon: markRaw(Phone),
    number: '01',
    title: 'Contactez-nous',
    description: 'Joignez-nous par téléphone, WhatsApp ou email pour initier votre demande.',
  },
  {
    icon: markRaw(ClipboardList),
    number: '02',
    title: 'Expliquez votre demande',
    description: 'Nous préparons et assurons le suivi rigoureux de toutes vos démarches.',
  },
  {
    icon: markRaw(CheckCircle2),
    number: '03',
    title: 'Résultat rapide',
    description: 'Service complet livré avec professionnalisme dans les meilleurs délais.',
  },
]
</script>

<template>
  <section id="process" class="border-b border-slate-200 bg-slate-50">
    <div class="mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8">
      <div class="mx-auto max-w-2xl text-center">
        <h2 class="text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">
          Comment ça marche
        </h2>
        <p class="mt-4 text-base text-slate-600">
          Un processus simple et transparent en trois étapes.
        </p>
      </div>

      <div class="mt-16 grid grid-cols-1 gap-6 md:grid-cols-3">
        <div
          v-for="step in steps"
          :key="step.number"
          class="rounded-lg border border-slate-200 bg-white p-8 shadow-sm"
        >
          <div class="flex items-center justify-between">
            <div class="inline-flex h-10 w-10 items-center justify-center rounded-md bg-blue-700 text-white">
              <component :is="step.icon" class="h-5 w-5" :stroke-width="2" />
            </div>
            <span class="text-sm font-semibold text-slate-300">{{ step.number }}</span>
          </div>
          <h4 class="mt-6 text-lg font-semibold text-slate-900">{{ step.title }}</h4>
          <p class="mt-2 text-sm leading-relaxed text-slate-600">{{ step.description }}</p>
        </div>
      </div>
    </div>
  </section>
</template>
